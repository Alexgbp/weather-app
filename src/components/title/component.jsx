import React from 'react'

export default function TitleComponent({ title }) {
    return (
        <h2 style={{
            textAlign: "center",
            color: "#3fa9bb",
            margin: "0"
        }}>{title}</h2>
    )
}
